/** @type {import('stylelint').Config} */
const config = {
    plugins: [],

    extends: ['./tools/stylelint/stylelintrc_core.cjs'],
};

// eslint-disable-next-line import/no-default-export
export default config;
