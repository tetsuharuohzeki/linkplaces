{
    "extends": "../../tsconfig.base.project_reference.json",
    "compilerOptions": {
        // Allow to runnable in worker (does not allow to touch DOM).
        "lib": ["ESNext", "WebWorker"],
        "outDir": "${configDir}/__dist",
        "rootDir": "${configDir}/src"
    },
    "include": [
        // @prettier-ignore
        "**/*"
    ],
    "references": [
        { "path": "../foundation" },
        { "path": "../webext_types" }
    ]
}
