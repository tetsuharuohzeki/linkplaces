{
    "extends": "../../tsconfig.base.project_reference.json",
    "compilerOptions": {
        // Disallow to touch any DOM object.
        "lib": ["ES2024"],

        "outDir": "${configDir}/__dist",
        "rootDir": "${configDir}/src"
    },
    "include": [
        // @prettier-ignore
        "**/*"
    ],
    "references": [
        { "path": "../foundation" },
        { "path": "../webext_types" }
    ]
}
